┌─────────────────────────────────────────────────────────────┐
│                    PHASE 3 COMPONENTS                        │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              AGENT ORCHESTRATOR                       │   │
│  │  • Dynamic agent discovery                            │   │
│  │  • Task routing with capability matching              │   │
│  │  • Parallel execution management                      │   │
│  │  • State tracking & recovery                          │   │
│  └─────────────────────────────────────────────────────┘   │
│                           │                                  │
│           ┌───────────────┼───────────────┐                  │
│           ▼               ▼               ▼                  │
│  ┌───────────────┐ ┌───────────────┐ ┌───────────────┐      │
│  │  RECON AGENT  │ │ SCANNER AGENT │ │   CUSTOM      │      │
│  │  • Network    │ │ • Vuln scan   │ │   AGENTS      │      │
│  │  • Port scan  │ │ • Web scan    │ │  (Pluggable)  │      │
│  └───────────────┘ └───────────────┘ └───────────────┘      │
│                           │                                  │
│                           ▼                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  TOOL ROUTER                          │   │
│  │  • Dynamic tool discovery                            │   │
│  │  • Capability-based selection                        │   │
│  │  • Load balancing & rate limiting                    │   │
│  │  • Cost tracking & budget enforcement                │   │
│  └─────────────────────────────────────────────────────┘   │
│                           │                                  │
│                           ▼                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                  WORKER POOL                          │   │
│  │  • Docker container management                       │   │
│  │  • Auto-scaling based on load                        │   │
│  │  • Resource isolation (CPU/Memory)                   │   │
│  │  • Health checking & recovery                         │   │
│  │  • Network isolation                                  │   │
│  └─────────────────────────────────────────────────────┘   │
│                           │                                  │
│                           ▼                                  │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              TOOL REGISTRY                            │   │
│  │  • Dynamic tool discovery from Docker images         │   │
│  │  • YAML configuration support                        │   │
│  │  • Capability indexing                               │   │
│  │  • Version management                                │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                              │
└─────────────────────────────────────────────────────────────┘


src/
├── orchestrator/
│   ├── __init__.py
│   ├── agent_orchestrator.py      # Main orchestrator
│   └── communication_bus.py        # Agent communication
│
├── agents/
│   ├── domain/
│   │   ├── __init__.py
│   │   ├── base_domain_agent.py    # Base class for all domain agents
│   │   ├── recon_agent.py          # Reconnaissance agent
│   │   ├── scanner_agent.py        # Vulnerability scanner agent
│   │   └── exploit_agent.py        # Exploitation agent (future)
│   │
│   ├── collaboration/
│   │   ├── __init__.py
│   │   └── memory_bus.py           # Agent communication bus
│   │
│   └── discovery.py                 # Dynamic agent discovery
│
├── tools/
│   ├── __init__.py
│   ├── tool_router.py               # Routes tasks to tools
│   ├── tool_registry.py             # Registry of available tools
│   ├── tool_discovery.py             # Dynamic tool discovery
│   ├── tool_registration.py          # Tool registration service
│   ├── rate_limiter.py              # Rate limiting
│   └── cost_tracker.py              # Cost tracking
│
├── workers/
│   ├── __init__.py
│   ├── worker_pool.py                # Docker worker pool
│   ├── container_manager.py          # Docker container management
│   ├── network_manager.py            # Network isolation
│   └── resource_monitor.py           # Resource monitoring
│
└── recovery/
    ├── __init__.py
    ├── retry_manager.py              # Retry logic
    ├── circuit_breaker.py            # Circuit breaker pattern
    ├── fallback_manager.py           # Fallback strategies
    └── escalation_manager.py         # Escalation policies


✅ Phase 3 Features Implemented
Feature	Status	Description
Dynamic Tool Discovery	✅	From Docker labels and YAML files
Tool Registry	✅	Capability-based indexing
Worker Pool	✅	Docker container management
Container Isolation	✅	Resource limits, network isolation
Auto-scaling	✅	Scale workers based on load
Health Checking	✅	Periodic worker health checks
Tool Router	✅	Routes tasks to appropriate tools
Rate Limiting	✅	Prevents abuse
Cost Tracking	✅	Real-time cost monitoring
Domain Agents	✅	Recon, Scanner with real logic
Agent Discovery	✅	Auto-discovers agent classes
Memory Bus	✅	Agent collaboration
Recovery Components	✅	Retry, circuit breaker, fallback
Agent Orchestrator	✅	Routes tasks to agents



    # 4. Test execution with real Docker
curl -X POST http://localhost:8000/api/v1/hybrid/execute \
  -H "Content-Type: application/json" \
  -d '{
    "goal": "Scan example.com for open ports",
    "target": "example.com",
    "priority": "high"
  }'

# 5. Check worker pool status
curl http://localhost:8000/api/v1/workers/stats  # You'll need to add this endpoint

# 6. Check tool registry
curl http://localhost:8000/api/v1/tools/list  # You'll need to add this endpoint

# 1. Check available tools
curl http://localhost:8000/api/v1/workers/tools

# Expected response:
{
  "tenant_id": "default",
  "total_tools": 4,
  "tools": [
    {
      "name": "gobuster",
      "version": "3.6.0",
      "capabilities": ["directory_bruteforce", "dns_enumeration"],
      ...
    },
    ...
  ]
}

# 2. Check worker pool status
curl http://localhost:8000/api/v1/workers/stats

# 3. Check agent status
curl http://localhost:8000/api/v1/agents/status

# 4. Execute a scan
curl -X POST http://localhost:8000/api/v1/hybrid/execute \
  -H "Content-Type: application/json" \
  -d '{
    "goal": "Scan example.com for open ports",
    "target": "example.com",
    "priority": "high"
  }'

# You'll get a response like:
{
  "process_id": "proc_abc123...",
  "status": "pending",
  "goal": "Scan example.com for open ports",
  "target": "example.com",
  "created_at": "2024-01-01T00:00:00.000Z",
  "updated_at": null,
  "estimated_completion": null,
  "message": null
}

# 5. Check execution status (replace with your process_id)
curl http://localhost:8000/api/v1/hybrid/status/proc_abc123...

# 6. Monitor the execution in real-time (you'll see logs in the terminal)

# 7. List all executions
curl http://localhost:8000/api/v1/hybrid/list